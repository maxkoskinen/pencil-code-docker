---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pencil-output
  namespace: pencil
  labels:
    app: pencil-code
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ""
  resources:
    requests:
      storage: 10Gi
